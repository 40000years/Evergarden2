# Allies: the caster, their tamed animals, and members of their main-scoreboard team.
pvp: true
max-active-effects: 128
max-targets-per-effect: 32
# Bound the new cinematic spells' packet work independently of older spells.
mythic-max-active-per-world: 4
mythic-terrain:
  # Native, non-flowing liquids; ground returns from the outside inward.
  radius: 16
  duration-seconds: 15
  # 4 = Poison V, refreshed for 5 seconds while enemies touch the water.
  poison-amplifier: 4
meteor:
  ignite-terrain: true
  # Explosions damage entities but never remove terrain blocks.
  damage: 135.0
damage:
  solar-beam: 32.0
  solar-apocalypse: 180.0
  solar-lava: 12.0
  chronos-blade: 24.0
  chronos-shatter: 100.0
  chronos-poison: 12.0
  sonic-boom: 112.5
  blaze-barrage: 37.5
  guardian-tidal-burst: 67.5
  poison-impact: 30.0
  lightning: 90.0
  lightning-secondary: 45.0
  frost-shatter: 52.5
  earth-wall-rupture: 37.5
  dragon-per-second: 45.0
  wither-skull: 60.0
  shulker-impact: 30.0
  shulker-singularity-burst: 180.0
  void-collapse: 67.5
  shadow-step-rupture: 37.5
  soul-nova: 52.5
  shroud-ambush: 75.0
  natures-bloom-thorns: 30.0
# Automatic extra stage from the same cast, with no additional mana or cooldown.
# Existing customized base damage settings are preserved when upgrading.
follow-up:
  damage:
    lightning_strike: 27.0
    frost_nova: 37.5
    shadow_step: 30.0
    natures_bloom: 22.5
    earth_wall: 37.5
    dragons_breath: 45.0
    void_pull: 30.0
    sonic_boom: 37.5
    blaze_barrage: 30.0
    wither_ray: 22.5
    shulker_levitation: 30.0
    meteor_strike: 22.5
    iron_armor: 37.5
    vex_legion: 37.5
    guardian_beam: 30.0
# Bundled pack is automatically offered one tick after joining.
flying-staff:
  enabled: true
  diagnostics:
    # One read-only Bedrock flight trace per player per restart, at most 120 seconds.
    # Stops 20 seconds after dismount; writes StaffTrace lines to the server log.
    enabled: true
  # Old default Turbo is the new normal speed; custom base speeds are kept.
  speed-version: 3
  horizontal-speed: 0.486
  vertical-speed: 0.288
  turbo-multiplier: 4.0
  mana-per-second: 2.0
  # Normal = 2 mana/sec; Turbo = 3 mana/sec with the default multiplier.
  turbo-mana-multiplier: 1.5
  anticheat:
    # Permission is granted only while riding and removed on dismount/quit.
    # Add other plugin permissions only after checking that plugin's documentation.
    bypass-permissions:
      GrimAC: [grim.disabled]
      NoCheatPlus: [nocheatplus.checks.moving.vehicle]
resource-pack:
  enabled: true
  # The bundled pack includes the flying staff; set a custom URL/SHA-1 only
  # when that URL serves this exact pack revision.
  url: ''
  sha1: ''
  required: false
  host:
    enabled: true
    bind: '0.0.0.0'
    # Must be reachable by Java clients when using the bundled pack.
    port: 8187
    # Empty = hostname the player joined. Set explicitly for proxies/SRV records.
    public-host: ''
    # Optional HTTP(S) reverse-proxy base URL. The pack path is appended.
    public-url: ''
  geyser:
    # Copies Advance Magic's own files during onLoad, before Geyser starts.
    auto-install: true

compatibility:
  # false: player roots use Slowness V; mobs retain the full root.
  # true: also force player positions/velocity; test with your anticheat first.
  hard-player-roots: false
  # Server-sent, capped knockback/pull. Set false for strict servers (damage is unchanged).
  player-spell-velocity: true
  max-player-horizontal-velocity: 1.1
  max-player-vertical-velocity: 0.9
